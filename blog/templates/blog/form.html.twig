{% extends 'base.html.twig' %}

{% block title %}form category creation{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_row(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
    <a href="{{ path('app_index') }}">
        Back to homepage.
    </a>
{% endblock %}